services:
  mongo:
    image: mongo:7
    container_name: shopkeeper-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: shopkeeper
    volumes:
      - shopkeeper_mongo_data:/data/db

volumes:
  shopkeeper_mongo_data:
